package classes;

public class Card {
  // Instance Variables

  // Set up enums to represent each set of features
  private enum Shape {
    OVAL,
    SQUIGGLES,
    DIAMONDS
  }

  private enum Color {
    RED,
    PURPLE,
    GREEN
  }

}
